# CSP Policy

This site uses a strict default-src self policy with explicit allowances for Cloudflare Pages assets, analytics endpoints, and trusted ad delivery.

Known AI crawlers are allowed through robots and edge response headers when they request public pages.
